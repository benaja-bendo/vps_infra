---
- name: Créer le dossier pour CloudBeaver
  ansible.builtin.file:
    path: /opt/docker/cloudbeaver
    state: directory
    mode: '0755'
    owner: "{{ ansible_user | default('root') }}"
    group: docker

- name: Copier le fichier docker-compose pour CloudBeaver
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: /opt/docker/cloudbeaver/docker-compose.yml
    mode: '0644'

- name: Démarrer CloudBeaver
  community.docker.docker_compose_v2:
    project_src: /opt/docker/cloudbeaver
    state: present
    pull: missing
